Для запуска тестов необходимо выполнить следующие команды в терминале Bash, находясь в директории проекта:
```
pythom -m venv venv
source venv/Scripts/activate
python -m unittest test_scheduler.py
```
